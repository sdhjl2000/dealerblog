@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper, IList<DealerBlog.Entity.Tag> tags)
{
  foreach (var tag in tags)
  {
    <div class="tags-div">@DealerBlog.Web.Web.ActionLinkExtensions.TagLink(htmlHelper, tag)</div>     
  }
}